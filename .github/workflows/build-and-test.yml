on:
  push:
    branches:
    - "main"
    tags:
    - "**"
  pull_request:

jobs:
  build_and_test:
    runs-on: "ubuntu-latest"
    steps:
    - uses: "actions/checkout@v4"
    - uses: "actions/setup-go@v5"
      with:
        go-version: "1.22"
    - run: "make all"
